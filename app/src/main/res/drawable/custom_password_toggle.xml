<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Saat password terlihat (checked) -->
    <item android:drawable="@drawable/mata_bukah" android:state_checked="true"/>
    <!-- Saat password tersembunyi (default) -->
    <item android:drawable="@drawable/mata_tutuph" />
</selector>
